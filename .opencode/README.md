# OpenCode Configuration

This directory contains the configuration for OpenCode.

## Local Model Setup
The configuration points to a local `llama.cpp` (or compatible) server running at `http://127.0.0.1:8080/v1`.

- **Model ID**: `local-model`
- **Name**: `Local Qwen3 Coder`